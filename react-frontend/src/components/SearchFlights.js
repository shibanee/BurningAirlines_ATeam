import React, { Component } from "react";

class SearchFlights extends Component {
  render() {
    return (
      <div>
        <input type="text" />
        <button type="sumbit">Search Flights</button>
      </div>
    );
  }
}

export default SearchFlights;
